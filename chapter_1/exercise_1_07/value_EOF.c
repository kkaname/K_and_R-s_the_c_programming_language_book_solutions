#include <stdio.h>

/* Exercise 1.07: Write a program to print the value of EOF. */

int main(void) {
    printf("The value of EOF is %d\n", EOF); /* Prints the value of EOF as -1. */

    return 0;
}
